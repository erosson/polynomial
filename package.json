{
    "name": "erosson-typescript-libs",
    "private": true,
    "scripts": {
        "build": "yarn workspaces run build",
        "test": "yarn workspaces run test --run",
        "rootdocs": "rm -rf dist && mkdir -p dist && yarn workspaces run rootdocs"
    },
    "workspaces": [
        "packages/*"
    ]
}